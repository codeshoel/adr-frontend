# Backend API base URL. NOTE: this is inlined into the client bundle at BUILD
# time — you must rebuild the frontend whenever it changes.
# Local dev:
# NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
# VPS production (backend served on :1906):
NEXT_PUBLIC_API_URL=http://79.143.191.22:1906/api/v1
